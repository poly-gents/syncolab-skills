# Skills

This directory contains one folder per skill.

Recommended layout:

- `skills/<skill-id>/SKILL.md`
- `skills/<skill-id>/examples/` for usage examples when needed
- `skills/<skill-id>/assets/` for diagrams or supporting files when needed

Use `skills/_template/SKILL.md` as the starting point for new skill packages.
