name: Build Components
version: 1.0.0
type: methodical
description:
  capability: Implement reusable UI components with proper testing
  trigger_conditions: Use when Implement reusable UI components with proper testing
  scope_boundaries: Focused on Build Components workflows via available integrations;
    does not replace human approval for destructive or production changes.
  key_domain_words:
  - build
  - components
  - Build Components
  - react
  - storybook
  - typescript
tags:
- build-components
- react
- storybook
- typescript
- vscode
relationships:
  skills:
  - name: create-skill
    type: references
  tool_sets:
  - name: react
    type: can-have
  - name: storybook
    type: can-have
  - name: typescript
    type: can-have
  - name: vscode
    type: can-have
